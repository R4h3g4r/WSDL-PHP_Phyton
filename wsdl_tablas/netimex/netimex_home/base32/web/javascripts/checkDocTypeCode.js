function checkDocTypeCode(comboElem)
{
	//javascript:setDocTypeCodeAndDesc(comboElem);
	//check and disable or enable the general id and version fields
	javascript:checkGIDAndVersionFields(comboElem);
}
